---
title: MCP backend API
description: HTTP endpoints used by the SHARC MCP server
---

These endpoints are shown for informational context only.

<Warning>
  Do not call these endpoints directly in normal usage. SHARC should be used through MCP tools only.
</Warning>

Use SHARC from your MCP client (Claude Code, Cursor, VS Code, Windsurf, and similar tools).

If you need direct inference API access, contact `contact@sharc.sh`.
